function url(t) { var a = t.split("/"); return a.pop(), a.join("/") }

function getData(t) { return $(t + " :input").map(function() { var t = $(this).prop("type"); return "checkbox" != t && "radio" != t || !this.checked ? "button" != t && "submit" != t ? $(this).val() : void 0 : $(this).val() }).join(",") }
var $t;
$(document).on("click", "#logout", function() { window.location.href = url(window.location.href) + "/controller/logout-controller.php" }), $(document).on("click", "#logout-modal", function() { $("#exampleModalLabel").text("Ready to Leave?"), $(".modal-label").text('Select "Logout" below if you are ready to end your current session.'), $(".modal-footer").html('<button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button><a class="btn btn-primary" id="logout">Logout</a>') }), $(document).on("input", "#input-category", function() { "" === $(this).val() ? $("#add-cat-btn").hide() : $("#add-cat-btn").show(500) }), $(document).on("click", "#open-cat", function() { $t = $("#cat-table").DataTable() }), $("#add-cat-btn").click(function() { $.ajax({ url: url(window.location.href) + "/controller/add-category.php", method: "POST", data: { submit: $(this).val(), category: $("#input-category").val() }, success: function(t) { var a = JSON.parse(t); if ($("#input-category").val(""), $("#add-cat-btn").hide(), $("#category-message").html('<div class="alert alert-' + a.status + ' role="alert" style="display:none">' + a.message + "</div>"), $(".alert").show(500), void 0 !== a.name) { var o = $('<a style="display: none" class="collapse-item" href="?p=inventory&cat=' + a.name + '">' + a.name + "</a>");
                o.insertAfter(".cat:last"), o.show(500);
                $t.row.add([a.id + "", a.name + "", '<button class="cat-del btn btn-danger btn-sm">Delete</button><button class="cat-up btn btn-success btn-sm">Update</button>']).draw();
                $t.draw() } } }) });